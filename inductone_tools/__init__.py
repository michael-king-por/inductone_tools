"""InductOne Tools Frappe app package metadata."""

# Frappe's `bench version` imports the app package and expects `__version__`
# when no branch-specific hook version is present. Keep this in sync with
# pyproject.toml.
__version__ = "3.0.0"
