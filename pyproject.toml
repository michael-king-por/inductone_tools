[project]
name = "inductone_tools"
version = "3.0.0"
description = "InductOne configuration tooling"
authors = [
    {name = "Plus One Robotics"}
]
dependencies = [
    "pypdf",
    "reportlab"
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"